######### Generated by Lwspanel #########
######### Ne pas modifier / Don't touch #########

# Redirections https jacquin.xyz
RewriteEngine On
RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteCond %{HTTPS} !on
RewriteCond %{HTTP_HOST} ^(?:www\.)?jacquin\.xyz(?:.*)$ [nc]
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [redirect=301,L]

######### End Generated by Lwspanel #########

# Enables rewriting URL

RewriteEngine on
RewriteBase /

# Hide index.html

RewriteCond %{REQUEST_URI} ^(.*)index\.html$ [NC]
RewriteRule ^ %1 [R=301,L]

# Hide .html, and then resolve .html-less url

RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.html [NC]
RewriteRule ^ %1 [R=301,L]

RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html

# Hide .pdf, and then resolve .pdf-less url

# RewriteCond %{THE_REQUEST} \s/+(.+?)\.pdf[\s?] [NC]
# RewriteRule ^ /%1 [R=302,L,NE]

# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteCond %{DOCUMENT_ROOT}/$1\.pdf -f [NC]
# RewriteRule ^(.+?)/?$ /$1.pdf [L]

# Unknown

# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)/$ $1 [R=301,L]

# Error handling

ErrorDocument 401 https://jacquin.xyz
ErrorDocument 402 https://jacquin.xyz
ErrorDocument 403 https://jacquin.xyz
ErrorDocument 404 https://jacquin.xyz

# Redirections
Redirect http://jacquin.xyz https://jacquin.xyz
